x-base-image: &base-image gitlab.ika.rwth-aachen.de:5050/fb-fi/ops/docker-base/ros2:latest
x-dev-image:  &dev-image  gitlab.ika.rwth-aachen.de:5050/fb-fi/simulation/carla/carla_its_converter:local-dev
x-run-image:  &run-image  gitlab.ika.rwth-aachen.de:5050/fb-fi/simulation/carla/carla_its_converter:local
x-command:    &command    ros2 launch carla_its_converter carla_its_converter_ros2.launch

# ==============================================================================

x-build: &build
  dockerfile: ./docker/docker-ros/Dockerfile
  context: ../
  args:
    BASE_IMAGE: *base-image
    COMMAND: *command
    GIT_HTTPS_USER: $GIT_HTTPS_USER
    GIT_HTTPS_PASSWORD: $GIT_HTTPS_PASSWORD

services:
  dev:
    image: *dev-image
    build:
      target: dev
      <<: *build
  run:
    image: *run-image
    build:
      target: run
      <<: *build
